'use client';

import Navbar from './Navbar';

const ConditionalNavbar = () => {
  // For now, always show the navbar
  // You can add conditional logic here if needed (e.g., hide on certain pages)
  return <Navbar />;
};

export default ConditionalNavbar;

